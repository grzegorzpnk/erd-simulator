apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmc.fullname" . }}
  labels:
    {{- include "pmc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.config.servicePort }}
      nodePort: 32138
  selector:
    {{- include "pmc.selectorLabels" . | nindent 4 }}
